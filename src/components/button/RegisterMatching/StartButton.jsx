import React from "react";
import "./StartButton.css";

const StartButton = () => {
    return (
        <button className="start-btn">
            시작하기
        </button>
    );
};

export default StartButton;
