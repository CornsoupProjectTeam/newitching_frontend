import React from "react";
import "./StartButton.css";

const StartButton = () => {
    return (
        <div className="start-btn">
            시작하기
        </div>
    );
};

export default StartButton;
